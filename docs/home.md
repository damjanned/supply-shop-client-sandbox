### Home Page

##### Client Components

1. **AppBar**: We need to use a `state` and `effect` in appbar to provide functionality of UI change of scrollbar so it is a **Client Component**.

##### Server Components

1. **Remaining Page**: We don't need any client specific features so its a server component with data for all three lists i.e. categories, suppliers and products fetched on server with a caching interval of **2 hours**.
